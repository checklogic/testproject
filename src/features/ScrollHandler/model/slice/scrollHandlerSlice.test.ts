import {
    scrollHandlerReducer,
    scrollHandlerActions,
} from './scrollHandlerSlice';
import { ScrollHandlerSchema } from '../types/scrollHandlerSchema';

describe('scrollHandlerSlice.test', () => {
    test('should', () => {
        // expect().toEqual();
    });
});
