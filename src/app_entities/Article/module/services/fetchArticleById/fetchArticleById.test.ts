import {
    fetchArticleByIdReducer,
    fetchArticleByIdActions,
} from './fetchArticleByIdSlice';
import { FetchArticleByIdSchema } from '../types/fetchArticleByIdSchema';

describe('fetchArticleByIdSlice.test', () => {
    test('should', () => {
        expect().toEqual();
    });
});
